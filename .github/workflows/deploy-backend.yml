name: Deploy Backend to DigitalOcean

on:
  push:
    branches: [main]
    paths:
      - 'backend/**'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Copy backend files to Droplet
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.DROPLET_HOST }}
          username: ${{ secrets.DROPLET_USER }}
          key: ${{ secrets.DROPLET_SSH_KEY }}
          source: 'backend/*'
          target: '/home/parsons/backend/'
          strip_components: 1
          overwrite: true
          rm: false # Don't remove files that aren't in source

      - name: Restart service
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.DROPLET_HOST }}
          username: ${{ secrets.DROPLET_USER }}
          key: ${{ secrets.DROPLET_SSH_KEY }}
          command_timeout: 5m
          script: |
            cd /home/parsons/backend

            # Backup sessions before updating (safety measure)
            if [ -d "data/sessions" ]; then
              echo "Sessions exist, keeping them safe"
            fi

            source venv/bin/activate
            pip install -r requirements.txt --quiet
            systemctl restart parsons
            sleep 2
            systemctl status parsons --no-pager
